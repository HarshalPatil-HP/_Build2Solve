import PlaceholderPage from '../../components/common/PlaceholderPage';

export default function ScanResultPage() {
  return (
    <PlaceholderPage
      title="Scan result"
      route="/scan/:id/result"
      purpose="Field-by-field pass/fail/exempt/needs-review result."
    />
  );
}
