import PlaceholderPage from '../../components/common/PlaceholderPage';

export default function ScanPage() {
  return (
    <PlaceholderPage
      title="Scan a product"
      route="/scan"
      purpose="Upload or capture 1–4 label images and run a compliance scan."
    />
  );
}
