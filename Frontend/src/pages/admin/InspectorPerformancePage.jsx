import PlaceholderPage from '../../components/common/PlaceholderPage';

export default function InspectorPerformancePage() {
  return (
    <PlaceholderPage
      title="Inspector performance"
      route="/admin/inspectors/:id/performance"
      purpose="Scans, cases closed, average time to resolution."
    />
  );
}
