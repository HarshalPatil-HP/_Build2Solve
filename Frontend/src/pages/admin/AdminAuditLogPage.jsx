import PlaceholderPage from '../../components/common/PlaceholderPage';

export default function AdminAuditLogPage() {
  return (
    <PlaceholderPage
      title="Audit log"
      route="/admin/audit-log"
      purpose="Who changed what and when — read-only accountability trail."
    />
  );
}
