import PlaceholderPage from '../../components/common/PlaceholderPage';

export default function InspectorScanPage() {
  return (
    <PlaceholderPage
      title="Field inspection scan"
      route="/inspector/scan"
      purpose="Scan flow plus GPS auto-tag and case formalization."
    />
  );
}
