import PlaceholderPage from '../../components/common/PlaceholderPage';

export default function InspectorCasesPage() {
  return (
    <PlaceholderPage
      title="My cases"
      route="/inspector/cases"
      purpose="Inspector cases filtered by draft / locked / resolved."
    />
  );
}
