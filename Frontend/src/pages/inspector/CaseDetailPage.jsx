import PlaceholderPage from '../../components/common/PlaceholderPage';

export default function CaseDetailPage() {
  return (
    <PlaceholderPage
      title="Case detail"
      route="/inspector/cases/:id"
      purpose="Evidence, lock case, append-only addendum."
    />
  );
}
