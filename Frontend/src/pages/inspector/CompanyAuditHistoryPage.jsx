import PlaceholderPage from '../../components/common/PlaceholderPage';

export default function CompanyAuditHistoryPage() {
  return (
    <PlaceholderPage
      title="Company audit history"
      route="/inspector/companies/:id/history"
      purpose="Every past scan and violation for one company."
    />
  );
}
