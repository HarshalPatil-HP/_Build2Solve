import PlaceholderPage from '../../components/common/PlaceholderPage';

export default function UserDashboardPage() {
  return (
    <PlaceholderPage
      title="Consumer dashboard"
      route="/dashboard"
      purpose="Scan CTA plus recent scans and complaints."
    />
  );
}
